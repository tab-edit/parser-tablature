# Missing dividers

a--|
a3--|

==>

Tablature(TabSegment(
    TabSegmentLine(TabString(
        MeasureLineName, MeasureLine(InsertDivider), DividerEnd
    )),
    TabSegmentLine(TabString(
        MeasureLineName, MeasureLine(InsertDivider), DividerEnd
    ))
))

# Varying divider thickness

a|----|
b||---|
c|---||
d||--||

==>

Tablature(TabSegment(
    TabSegmentLine(TabString(
        MeasureLineName, MeasureLine(DividerOpen), DividerEnd
    )),
    TabSegmentLine(TabString(
        MeasureLineName, MeasureLine(DividerOpen), DividerEnd
    )),
    TabSegmentLine(TabString(
        MeasureLineName, MeasureLine(DividerOpen), DividerEnd
    )),
    TabSegmentLine(TabString(
        MeasureLineName, MeasureLine(DividerOpen), DividerEnd
    ))
))
